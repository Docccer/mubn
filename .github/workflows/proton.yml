name: proton-v7
on:
  workflow_dispatch:

jobs:
  register:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/setup-node@v4
        with:
          node-version: '20'
      
      - name: Install Playwright
        run: |
          npm init -y
          npm install playwright
          npx playwright install chromium --with-deps
      
      - name: Register ProtonMail
        run: |
          cat > register.js << 'JSEOF'
          const { chromium } = require('playwright');
          const crypto = require('crypto');
          
          const rand = crypto.randomBytes(4).toString('hex');
          const username = `hxtest${rand}`;
          const password = 'HxTest2026!@#';
          
          console.log(`[*] Username: ${username}@proton.me`);
          console.log(`[*] Password: ${password}`);
          
          (async () => {
              const browser = await chromium.launch({
                  headless: true,
                  args: ['--no-sandbox', '--disable-blink-features=AutomationControlled']
              });
              
              const context = await browser.newContext({
                  locale: 'en-US',
                  userAgent: 'Mozilla/5.0 (Windows NT 10.0; Win64; x64) AppleWebKit/537.36 (KHTML, like Gecko) Chrome/126.0.0.0 Safari/537.36'
              });
              
              const page = await context.newPage();
              
              await page.addInitScript(() => {
                  Object.defineProperty(navigator, 'webdriver', { get: () => false });
              });
              
              console.log('[*] Navigating...');
              await page.goto('https://account.proton.me/signup?plan=free', { 
                  waitUntil: 'domcontentloaded', 
                  timeout: 30000 
              });
              await page.waitForTimeout(12000);
              
              console.log(`[*] Title: ${await page.title()}`);
              await page.screenshot({ path: 'step1.png', fullPage: true });
              
              // Dump inputs
              const inputs = await page.evaluate(() => {
                  return Array.from(document.querySelectorAll('input')).map((i, idx) => ({
                      idx, id: i.id, type: i.type, testid: i.getAttribute('data-testid'),
                      visible: i.offsetParent !== null, disabled: i.disabled
                  }));
              });
              console.log(`[*] Inputs: ${JSON.stringify(inputs)}`);
              
              // Type into inputs using keyboard simulation
              // Focus the first input (username) using evaluate, then type with keyboard
              await page.evaluate(() => {
                  const el = document.querySelector('[data-testid="input-input-element"]');
                  if (el) { el.focus(); el.click(); }
              });
              await page.waitForTimeout(500);
              
              // Clear any existing text and type username
              await page.keyboard.press('Control+a');
              await page.keyboard.type(username, { delay: 50 });
              await page.waitForTimeout(500);
              
              // Verify username was typed
              const userVal = await page.evaluate(() => {
                  const el = document.querySelector('[data-testid="input-input-element"]');
                  return el ? el.value : 'NOT FOUND';
              });
              console.log(`[*] Username value: ${userVal}`);
              
              // Tab to password field or focus it
              await page.evaluate(() => {
                  const els = document.querySelectorAll('[data-testid="input-input-element"]');
                  if (els.length >= 2) { els[1].focus(); els[1].click(); }
              });
              await page.waitForTimeout(500);
              
              await page.keyboard.press('Control+a');
              await page.keyboard.type(password, { delay: 50 });
              await page.waitForTimeout(500);
              
              // Verify password
              const pwdVal = await page.evaluate(() => {
                  const els = document.querySelectorAll('[data-testid="input-input-element"]');
                  return els.length >= 2 ? els[1].value : 'NOT FOUND';
              });
              console.log(`[*] Password value: ${pwdVal.substring(0, 3)}***`);
              
              await page.screenshot({ path: 'step2.png', fullPage: true });
              
              // Check for third input (repeat password)
              const inputCount = await page.evaluate(() => document.querySelectorAll('[data-testid="input-input-element"]').length);
              if (inputCount >= 3) {
                  console.log('[*] Found repeat password field');
                  await page.evaluate(() => {
                      const els = document.querySelectorAll('[data-testid="input-input-element"]');
                      els[2].focus(); els[2].click();
                  });
                  await page.waitForTimeout(300);
                  await page.keyboard.type(password, { delay: 50 });
              }
              
              // Click submit
              const clicked = await page.evaluate(() => {
                  const btns = Array.from(document.querySelectorAll('button'));
                  const targets = ['start using', 'create account', 'get started', 'sign up', 'next', 'continue'];
                  for (const btn of btns) {
                      const text = btn.innerText.trim().toLowerCase();
                      if (targets.some(t => text.includes(t))) {
                          btn.click();
                          return btn.innerText.trim();
                      }
                  }
                  return null;
              });
              console.log(`[*] Clicked: ${clicked}`);
              
              await page.waitForTimeout(10000);
              
              console.log(`[*] After submit - Title: ${await page.title()}`);
              console.log(`[*] After submit - URL: ${page.url()}`);
              
              const bodyText = await page.evaluate(() => document.body.innerText.substring(0, 800));
              console.log(`[*] Body: ${bodyText}`);
              
              await page.screenshot({ path: 'step3.png', fullPage: true });
              
              // Check verification
              const verify = await page.evaluate(() => {
                  const t = document.body.innerText;
                  const h = document.body.innerHTML.toLowerCase();
                  return {
                      phone: t.includes('phone') || t.includes('Phone'),
                      captcha: h.includes('captcha') || h.includes('recaptcha'),
                      recovery: t.includes('recovery'),
                      success: t.includes('Inbox') || t.includes('Welcome'),
                      sms: t.includes('SMS')
                  };
              });
              console.log(`[*] Verification: ${JSON.stringify(verify)}`);
              
              // Skip steps
              for (let s = 0; s < 5; s++) {
                  const skipped = await page.evaluate(() => {
                      const btns = Array.from(document.querySelectorAll('button, a'));
                      for (const btn of btns) {
                          const text = btn.innerText.trim().toLowerCase();
                          if (text === 'skip' || text === 'skip for now' || text === 'maybe later') {
                              btn.click(); return btn.innerText.trim();
                          }
                      }
                      return null;
                  });
                  if (skipped) { console.log(`[*] Skipped: "${skipped}"`); await page.waitForTimeout(5000); }
                  else break;
              }
              
              // Final
              console.log(`\n=== FINAL ===`);
              console.log(`Title: ${await page.title()}`);
              console.log(`URL: ${page.url()}`);
              console.log(`Body: ${await page.evaluate(() => document.body.innerText.substring(0, 500))}`);
              console.log(`\n=== CREDENTIALS ===`);
              console.log(`Email: ${username}@proton.me`);
              console.log(`Password: ${password}`);
              
              await page.screenshot({ path: 'final.png', fullPage: true });
              await browser.close();
          })().catch(e => { console.error('[-] Fatal:', e.message); process.exit(1); });
          JSEOF
          node register.js
      
      - name: Upload artifacts
        if: always()
        uses: actions/upload-artifact@v4
        with:
          name: proton-v7
          path: '*.png'
          retention-days: 1
